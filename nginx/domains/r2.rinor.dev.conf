#  server {
 #   server_name n.ofli.io;
#
 #   location ^~ /.well-known/acme-challenge/ {
  #      root /var/www/certs;
   # }
#
 #   location / {
  #    return 200 'test one';
   # }
 # }

server {
    server_name  r2.rinor.dev;

    listen 443 ssl; # managed by Certbot

    # RSA certificate
    ssl_certificate /etc/letsencrypt/live/r2.rinor.dev/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/r2.rinor.dev/privkey.pem; # managed by Certbot

    location / {
      return 200 'success friend';
    }

    # Redirect non-https traffic to https
    if ($scheme != "https") {
        return 301 https://$host$request_uri;
    } # managed by Certbot
}

